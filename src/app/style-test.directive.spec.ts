import { StyleTestDirective } from './style-test.directive';

describe('StyleTestDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleTestDirective();
    expect(directive).toBeTruthy();
  });
});
