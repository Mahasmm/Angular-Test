import { StyleTestsDirective } from './style-tests.directive';

describe('StyleTestsDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleTestsDirective();
    expect(directive).toBeTruthy();
  });
});
